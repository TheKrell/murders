We analyze US gun murder rate collected by FBI.

download-data.R - download csv to data directory

wrangle-data.R - creates a derived dataset and saves as R object in rda directory

analysis.R - generates a plot and saves it to figs directory